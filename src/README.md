This directory is for your React source files (Part 3). Place your JSX components here, starting with `CourseContent.jsx` as the entry point.

To compile and bundle:

```
npm run build:react
```

To auto-rebuild on changes:

```
npm run watch:react
```

The bundled output is written to `public/course-react.js`.
